{
  config,
  pkgs,
  lib,
  ...
}:

{
  home.file = {
    ".gemini/GEMINI.md".text = ''
      # Antigravity 汎用エンジニアリング規則 (Global Rules)

      ## 基本方針
      - **原則:** すべての回答、解説、コメント、ドキュメントは**日本語**を使用すること。
      - 以下のドキュメントも**日本語で作成する**：
        - 実装計画 (Implementation Plan)
        - 修正内容の確認 (Walkthrough)
        - タスクリスト (Task List)
      - **例外:** エラーメッセージ、ログ出力、システム識別子については英語を許可する。

      ## ドキュメント生成ルール
      - **複雑なコード変更や新規機能の実装を伴わない** 単純な質問や情報取得タスクの場合、**ドキュメントは生成しない**こと。
      - 特に、モデルが **Fast** の場合は、ドキュメント生成の省略を推奨する。
    '';
  };
}
